@attribute [Authorize]

<aside class="sentinel-panel hidden w-64 flex-shrink-0 flex-col gap-8 rounded-3xl p-6 shadow-sm lg:flex">
    <div class="flex items-center gap-3">
        <div class="flex h-10 w-10 items-center justify-center rounded-2xl bg-slate-900 text-white">
            <span class="text-lg font-semibold">S</span>
        </div>
        <div>
            <div class="text-lg font-semibold">Sentinel</div>
            <div class="text-xs text-slate-500">Autonomous monitoring</div>
        </div>
    </div>

    <nav class="flex flex-col gap-2 text-sm">
        <NavLink class="sentinel-pill rounded-xl px-3 py-2 font-medium text-slate-700 transition hover:text-slate-900" href="" Match="NavLinkMatch.All">
            Dashboard
        </NavLink>
        <NavLink class="sentinel-pill rounded-xl px-3 py-2 font-medium text-slate-700 transition hover:text-slate-900" href="logs">
            Live Logs
        </NavLink>
        <NavLink class="sentinel-pill rounded-xl px-3 py-2 font-medium text-slate-700 transition hover:text-slate-900" href="rules">
            Rules
        </NavLink>
        <NavLink class="sentinel-pill rounded-xl px-3 py-2 font-medium text-slate-700 transition hover:text-slate-900" href="actions">
            Actions
        </NavLink>
        <NavLink class="sentinel-pill rounded-xl px-3 py-2 font-medium text-slate-700 transition hover:text-slate-900" href="metrics">
            Metrics
        </NavLink>
        <NavLink class="sentinel-pill rounded-xl px-3 py-2 font-medium text-slate-700 transition hover:text-slate-900" href="configuration">
            Configuration
        </NavLink>
        <NavLink class="sentinel-pill rounded-xl px-3 py-2 font-medium text-slate-700 transition hover:text-slate-900" href="health">
            System Health
        </NavLink>
    </nav>

    <div class="mt-auto space-y-3 text-xs text-slate-500">
        <div class="flex items-center justify-between">
            <span>Region</span>
            <span class="font-medium text-slate-700">us-east-1</span>
        </div>
        <div class="flex items-center justify-between">
            <span>Mode</span>
            <span class="rounded-full bg-emerald-100 px-2 py-0.5 text-emerald-700">Live</span>
        </div>
        <AuthorizeView>
            <Authorized>
                <div class="flex items-center justify-between">
                    <span class="truncate">@context.User.Identity?.Name</span>
                    <a class="text-xs font-semibold text-slate-700" href="logout">Sign out</a>
                </div>
            </Authorized>
        </AuthorizeView>
    </div>
</aside>

